version: 1.0
runtime: nodejs22

build:
  commands:
    - npm ci
    - npm run build

run:
  command: npm run start
  network:
    port: 8080
  health_check:
    path: /health
    interval: 10
    timeout: 5
    healthy_threshold: 1
    unhealthy_threshold: 3
