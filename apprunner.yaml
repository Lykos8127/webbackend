version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - rm -rf node_modules package-lock.json
      - npm install --include=dev
    build:
      - npm ci --include=dev
      - npx medusa build
      - echo ">>> Admin build output <<<"
      - ls -R .medusa/server/public/admin || echo "— no admin folder found —"

run:
  # Tell App Runner which port your app listens on, and which env var to use
  network:
    port: 8080
    env: MEDUSA_PORT
  # The start command for your service
  command: >-
    pwd && ls -R . && ls -R .medusa/server && cd .medusa/server && npx medusa start
  env:
  # Runtime environment variables
  env:
  - name: NODE_ENV
    value: production
  - name: MEDUSA_ADMIN_BACKEND_URL
    value: https://api.myshop.com
  - name: MEDUSA_PORT
    value: "8080"

  #- name: PGSSLROOTCERT
   # value: certs/rds-ca.pem

  - name: DATABASE_URL
    value: postgres://postgres:E_b09.lgR@database-1.cjuao6qq8txz.eu-central-1.rds.amazonaws.com:5432/database-1?sslmode=no-verify

  - name: JWT_SECRET
    value: 4k2f6b1c5d3a837e5c0d2b8e6f2a3d8c
